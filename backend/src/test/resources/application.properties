# Test-only overrides. jwt.secret has no default in the main
# application.properties on purpose (a missing secret should fail startup
# loudly in real environments), so tests need their own fixed value —
# this one is not used anywhere outside the test run and signs nothing
# real.
jwt.secret=dGVzdC1vbmx5LXNlY3JldC1uZXZlci11c2VkLWluLXByb2R1Y3Rpb24tZW52
jwt.expiration-ms=86400000

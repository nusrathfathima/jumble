# Copy this file to .env.local (which is git-ignored) and adjust as needed.
# Vite only exposes variables prefixed with VITE_ to the browser.

# Where the frontend should send API requests during local development.
# Leave as-is when running the backend locally on its default port.
VITE_API_BASE_URL=http://localhost:8080
